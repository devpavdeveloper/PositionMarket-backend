spring:

  #Profile setting
  profiles:
    active: local

  #DataSource settings
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/devpav
    username: devpav
    password: devpav
