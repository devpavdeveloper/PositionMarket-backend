server:
  port: 8080

spring:
  #Profile setting
  profiles:
    active: prod

  #DataSource settings
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://postgres:5432/devpav
    username: devpav
    password: devpav